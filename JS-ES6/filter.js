const numbers = [12, 25, 121, 31, 13, 12, 52];
const bigNumbers = numbers.filter( number => number > 50);
console.log(bigNumbers);

// find Small
const smallNumber = numbers.filter( number => number < 50);
console.log(smallNumber);

