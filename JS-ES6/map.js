const array = [5, 6, 15, 32, 10];
const output = array.map(x => x * 2);
console.log(output);
const square = array.map(x => x * x);
console.log(square);

const friends = ['anik','asif', 'mahi', 'ovi', 'rakib', 'akash'];
const fLength = friends.map(friend => friend.length);
console.log(fLength);

