 /* backcode (``) using declare multiline string that called template string
 and dynamic string just need to declare ( ${} ) inside a template string */
 const firstName = 'Hablu';
 const lastName = 'Programmer';
 const fullName = `This person name is : ${firstName} ${lastName}`;

 console.log(fullName);

